.env
.env.*
# Excepción: la plantilla SÍ va al repo (no contiene secretos, solo el formato).
!.env.example
node_modules/
data.json
dist/
*.log

# PDFs de estudios subidos: datos clínicos, nunca al repo (se respaldan aparte).
uploads/

# Worktrees temporales de Claude Code: nunca al repo.
.claude/worktrees/
